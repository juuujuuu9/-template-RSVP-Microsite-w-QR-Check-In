# Neon Postgres (required)
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Resend (required for confirmation emails)
RESEND_API_KEY=re_xxxx
FROM_EMAIL=RSVP <noreply@yourdomain.com>

# Admin (required for /admin)
ADMIN_PASSWORD=your-secure-password
ADMIN_SESSION_SECRET=generate-with-openssl-rand-hex-32

# Hub / QR-Check-In (optional; omit to skip webhook and QR)
# HUB_URL=https://checkin.example.com
# HUB_WEBHOOK_KEY=same-as-hub-MICROSITE_WEBHOOK_KEY
# HUB_EVENT_SLUG=your-event-slug
